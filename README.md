# Productivity Tracker Extension

A Chrome extension to track time spent on productive vs distracting websites.

## Setup
1. Clone repo
2. Run `npm install`
3. Run `npm run build`
4. Load `dist` folder as unpacked extension in Chrome

## Features
- Track time in background
- Categorize sites in popup
- Dashboard with charts and goals
